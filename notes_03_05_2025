День 1:
    cd "путь"     перейти в указанную директорию 
    ls     выведение всех файлов в директории 
    sudo     от имени админа 
    pwd     вывести текущую директорию 
    .     текущая директория
    ..     директория выше
    ~      домашнаяя директория
    *      неограниченное количество любых символов
    ?      один любой символ
    mkdir      создать директорию
        -р      создание поддиректорий если надо            
    chmod      изменение свойств файла или директории
         +"буква"      добавление свойства в зависимости от буквы             
    stdin      вводные данные
    stdout      вывод 
    stderr      ошибка
    pipe "файл|файл|файл"      соединение в один конвейер с взятием выводы из предыдущего файл в следующий и передачей его как ввода 
    wget "ссылка на файл"      скачать файл по ссылке в интернете и сохранить в текущей директории 
        -P "путь до директории"      сохранить в указанной директории
        -с "ссылка"      докачать файл по ссылке
        --spider "ссылка"      проверить доступность ссылки
        -i "текстовый файл"      скачать файлы по указанным в файле ссылкам
    unzip "архив"    разархивировать .zip архив
    zip "название.zip" "файл1 файл2"    заархивировать файлы
    gunzip "архив"    разархивировать .gz архив с удалением первоначального архива 
    gzip "файл"    заархивировать файл (один файл) с расширением .gz
    bzip2 "файл"    сжимает файл в файл.bz2
    bunzip2 "файл.bz2"    распаковывает файл.bz2 в файл 
    tar    архиватор
        -с    создать архив
        -x    извлечь файлы из архива 
        -v    показать подробную информацию
        -f    указывает что работаем с файлами, всегда ставить в конце
        -t    показывает содержимое архива без извлечения
        -z    использует gzip для сжатия 
        -j    использует bzip2 для сжатия
существуют архиваторы и компиляторы, архиваторы собирают выбранные файлы и директории в один контейнер "архив" без сжатия. Компиляторы сжимают выбранные файлы 
    
